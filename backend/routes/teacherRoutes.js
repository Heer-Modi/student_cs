const express = require('express');
const router = express.Router();

// Define teacher-specific routes here
// For example:
// router.get('/profile', teacherController.getProfile);

module.exports = router;
