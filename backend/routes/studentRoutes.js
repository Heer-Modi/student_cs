const express = require('express');
const router = express.Router();

// Define student-specific routes here
// For example:
// router.get('/profile', studentController.getProfile);

module.exports = router;
